% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qtabs.R
\name{get_r6_fields}
\alias{get_r6_fields}
\title{#' @export
print.crosstabser_tabs <- function(x,
# unnest fields:
uf = c("d"),
n = 30,
...) {
field_list <- x |> purrr::map(get_r6_fields)
df <- tibble::tibble(x = field_list) |>
tidyr::unnest_wider(x)
if (length(uf) > 0) {
df <- df |>
tidyr::unnest_wider(uf)
# |>
#   dplyr::select(-matches("^Col\link{A-Z}$"))
}
print(df, n = n, ...)
}}
\usage{
get_r6_fields(r6_obj)
}
\description{
#' @export
print.crosstabser_tabs <- function(x,
# unnest fields:
uf = c("d"),
n = 30,
...) {
field_list <- x |> purrr::map(get_r6_fields)
df <- tibble::tibble(x = field_list) |>
tidyr::unnest_wider(x)
if (length(uf) > 0) {
df <- df |>
tidyr::unnest_wider(uf)
# |>
#   dplyr::select(-matches("^Col\link{A-Z}$"))
}
print(df, n = n, ...)
}
}
